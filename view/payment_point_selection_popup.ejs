<!DOCTYPE html>
<html lang="ko">

<head>
    <title>Hexagon Cinema</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nanum+Gothic:wght@400;700;800&display=swap" rel="stylesheet">
    <!-- icon -->
    <script src="https://kit.fontawesome.com/611c1b2c45.js" crossorigin="anonymous"></script>

    <link rel="stylesheet" href="style.css">
    <style>
        .earnPointPopup {
            width: 84rem;
            height: 54rem;

            position: absolute;
            left: calc(50% - 42rem);
            top: calc(50% - 27rem - 3rem);

            background-color: #ADADAD;

            display: flex;
            flex-wrap: wrap;
            align-content: flex-start;
            justify-content: center;
        }

        .popupBar {
            width: 100%;
            height: 4.5rem;
            background-color: #E6E6E6;
        }

        .earnPointTitle {
            width: 100%;

            font-size: 5rem;
            font-weight: 600;
            letter-spacing: .5rem;

            text-align: center;
        }

        .earnPointBtnWrapper {
            width: 100%;

            display: flex;
            justify-content: space-evenly;
        }

        .earnPointBtn {
            width: 36rem;
            height: 18rem;

            background-color: #EDEDED;

            font-size: 3.2rem;

            border: none;
        }

        .earnPointBtn:active {
            background-color: #d4d4d4;
        }

        .showEarnPoint {
            width: 50%;
            height: 10rem;
            background-color: #444444;
        }

        .showEarnPointText {
            font-size: 2.4rem;
            color: white;
            line-height: 10rem;

            text-align: center;
        }

        .earnPopupMarginTop {
            margin-top: 4rem;
        }
    </style>
</head>

<body>
    <div class="wrap">
        <section class="layoutArea">
            <!-- 여기에서 콘텐츠가 삽입됩니다. -->
            <div id="earnPointPopup" class="earnPointPopup">
                <div class="popupBar"></div>
                <h1 id="earnPointTitle" class="earnPointTitle earnPopupMarginTop">포인트를 적립하시겠습니까?</h1>
                <div id="earnPointBtnWrapper" class="earnPointBtnWrapper earnPopupMarginTop">
                    <button id="earnPointNo" class="earnPointBtn">아니오</button>
                    <button id="earnPointYes" class="earnPointBtn">예</button>
                </div>

                <div id="showEarnPoint" class="showEarnPoint earnPopupMarginTop">
                    <h2 id="showEarnPointText" class="showEarnPointText">적립예정포인트</h2>
                </div>
            </div>
    </div>
    <script>
        const earnPointYes = document.getElementById("earnPointYes");
        const earnPointNo = document.getElementById("earnPointNo");

        earnPointYes.addEventListener( "click", () => {
            window.location.href = "/inputPoint";
        });
        earnPointNo.addEventListener( "click", () => {
            window.location.href = "/buying";
        });
    </script>
</body>

</html>